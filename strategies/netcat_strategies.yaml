strategies:
  stealth:
    - "-v"          # Verbeux
    - "-z"          # Mode scan uniquement
    - "-w"          # Timeout (secondes)
    - "2"           # Timeout de 2 secondes
    - "-p"          # Indique que le port suit (traité dans le code)
    - "443"         # Port cible